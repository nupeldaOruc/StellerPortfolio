@model  List<Steller.Models.TblService>
<section id="service" class="section">
    <div class="container text-center">
        <h6 class="subtitle">Hizmetler</h6>
        <h6 class="section-title mb-4">Sizin için neler yapabilirim</h6>
        <p class="mb-5 pb-4">Sunduğum hizmetlere aşağıdan oluşabilirsininiz</p>

        <div class="row">

            @foreach (var item in Model)
            {
                <div class="col-sm-6 col-md-3 mb-4">
                    <div class="custom-card card border">
                        <div class="card-body">
                            <i class="@item.serviceliconUrl"></i>
                            <h5>@item.servicedName</h5>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>
